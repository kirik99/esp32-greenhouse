# 🌱 Growbox OS — Smart Greenhouse & Climate Controller

Комплексная IoT-система автоматизации микроклимата и мониторинга гроубокса / умной теплицы на базе микроконтроллера **Adafruit MatrixPortal ESP32-S3**, 6-канального блока силовых реле, набора климатических датчиков, UVC USB-камеры и серверного стека в **Docker**.

---

## 📌 Основные возможности

* **Мониторинг микроклимата в реальном времени:**
  * Температура воздуха, влажность, атмосферное давление (**BME280** по I2C);
  * Температура субстрата / грунта (**DS18B20** по OneWire);
  * Концентрация углекислого газа (**MH-Z19B** по UART);
* **Управление исполнительными устройствами (6 реле 220V/12V):**
  * Увлажнитель (генератор тумана);
  * Нагревательный элемент;
  * Обдув нагревателя;
  * Циркуляционный приточный вентилятор;
  * Вытяжной вентилятор;
  * Фитоосвещение (подсветка);
* **Визуальный контроль (USB Камера):**
  * Подключение UVC USB-камеры напрямую к ESP32-S3 через USB Host (GPIO 19/20);
  * Захват кадров в PSRAM буфер (650 КБ) каждые 10 минут или принудительно по кнопке;
  * Конвертация YUY2 в JPEG на сервере с сохранением архива с точной меткой времени;
* **Веб-интерфейс и телеметрия:**
  * Минималистичный тёмный дашборд (Linear / Vercel стиль) на Tailwind CSS;
  * Управление реле с обратной связью (интерактивные тумблеры-слайдеры);
  * Видоискатель камеры с возможностью делать снимок по требованию и галереей архива;
  * Непрерывное логирование телеметрии в базу данных временных рядов **InfluxDB 2.7**;
  * Обмен данными по легковесному протоколу **MQTT (Mosquitto)**.

---

## 🏗 Архитектура системы

```
                +------------------------------------+
                |    Adafruit MatrixPortal ESP32-S3   |
                |  (BME280, DS18B20, MH-Z19B, Cam)   |
                +-----------------+------------------+
                                  |
                   Wi-Fi / MQTT   | (Порт 1883)
                                  v
+------------------------------------------------------------------------+
|                             DOCKER SERVER                              |
|                                                                        |
|  +------------------------+             +---------------------------+  |
|  |   growbox_mosquitto    | <---------> |      growbox_bridge       |  |
|  |   (MQTT Broker :1883)  |             |  (Node.js REST & Worker)  |  |
|  +-----------+------------+             +------+-------------+------+  |
|              ^                                 |             |         |
|   WebSockets |                                 v             v         |
|              |                         +-----------+   +------------+  |
|              |                         | InfluxDB  |   |   images/  |  |
|              |                         |  (:8086)  |   |   (*.jpg)  |  |
|              |                         +-----------+   +------------+  |
|              v                                               |         |
|  +-----------------------------------------------------------+------+  |
|  |                      growbox_dashboard (Nginx :80)               |  |
|  |                                                                  |  |
|  |  /      -> Веб-интерфейс (index.html)                            |  |
|  |  /api/* -> Прокси в bridge:3001 (Снимки и ручное управление)     |  |
|  |  /mqtt  -> Прокси в mosquitto:9001 (WebSockets для браузера)     |  |
|  +----------------------------------+-------------------------------+  |
+-------------------------------------|----------------------------------+
                                      v
                        [ Браузер: ПК / Смартфон ]
```

---

## 🔌 Схема подключения оборудования (Распиновка)

### 1. Датчики и камера к ESP32-S3

| Устройство / Датчик | Выводы модуля | Пин ESP32-S3 | Питание | Примечание |
|---|---|---|---|---|
| **USB Камера** | D- / D+ / 5V / GND | **GPIO 19** (D-)<br>**GPIO 20** (D+) | 5V (VBUS) | USB Host UVC |
| **BME280** (Воздух) | SDA / SCL / VCC / GND | **GPIO 6** (SDA)<br>**GPIO 7** (SCL) | 3.3V | Шина I2C |
| **DS18B20** (Субстрат) | DATA / VCC / GND | **GPIO 4** | 3.3V | OneWire (подтяжка 4.7 кОм к 3.3V) |
| **MH-Z19B** (CO₂) | TX / RX / VIN / GND | **GPIO 17** (RX платы)<br>**GPIO 16** (TX платы) | **5V** | UART (5V обязательно для лампы) |

### 2. Модуль 6 реле (Слаботочная часть)

| Реле | Пин ESP32 | Потребитель | Логика |
|---|---|---|---|
| **IN 1** | **GPIO 1** | Увлажнитель (генератор тумана) | LOW = ВКЛ, HIGH = ВЫКЛ |
| **IN 2** | **GPIO 2** | Нагреватель | LOW = ВКЛ, HIGH = ВЫКЛ |
| **IN 3** | **GPIO 3** | Вентилятор нагревателя | LOW = ВКЛ, HIGH = ВЫКЛ |
| **IN 4** | **GPIO 8** | Вентилятор 1 (приток / циркуляция) | LOW = ВКЛ, HIGH = ВЫКЛ |
| **IN 5** | **GPIO 9** | Вентилятор 2 (вытяжка) | LOW = ВКЛ, HIGH = ВЫКЛ |
| **IN 6** | **GPIO 10** | Фитоподсветка | LOW = ВКЛ, HIGH = ВЫКЛ |

* **VCC реле** — к 5V платы.
* **GND реле** — к общей земле GND.
* **Силовая часть 220V:** Фаза (L) подключается в разрыв контактов **COM** (общий) и **NO** (нормально разомкнутый). Ноль (N) идёт к потребителям напрямую.

---

## 🚀 Быстрый старт

### 1. Прошивка микроконтроллера ESP32 (`growbox/`)

1. Откройте скетч `growbox/growbox.ino` в **Arduino IDE**.
2. Создайте файл `growbox/wifi_config.h` (скопировав `growbox/wifi_config.example.h`) и укажите ваши данные:
   ```cpp
   #define WIFI_SSID "Ваш_WiFi"
   #define WIFI_PASSWORD "Ваш_Пароль"
   #define MQTT_HOST "IP_СЕРВЕРА"   // Локальный IP компьютера или белый IP VPS
   #define MQTT_PORT 1883
   ```
3. В настройках Arduino IDE выберите:
   * **Плата:** `Adafruit MatrixPortal ESP32-S3`
   * **PSRAM:** `OPI PSRAM` *(Обязательно для буфера камеры!)*
   * **Порт:** Ваш COM-порт
4. Нажмите **Upload** (Загрузить).

### 2. Запуск сервера локально (Docker Desktop / WSL)

В папке `server/`:
```bash
docker compose up -d
```
После запуска откройте в браузере:
* **Дашборд управления:** [http://localhost](http://localhost) (или с телефона по IP компьютера: `http://192.168.x.x`)
* **База данных InfluxDB UI:** [http://localhost:8086](http://localhost:8086) *(логин `admin`, пароль `adminpassword`)*

---

## 🌐 Развертывание на удалённом сервере (VPS за 3 шага)

1. **Склонируйте репозиторий на VPS:**
   ```bash
   git clone https://github.com/kirik99/esp32-greenhouse.git
   cd esp32-greenhouse/server
   ```
2. **Запустите автоматический скрипт развертывания:**
   ```bash
   chmod +x deploy.sh
   ./deploy.sh
   ```
3. **Откройте порты в фаерволе (UFW):**
   ```bash
   sudo ufw allow 80/tcp     # Панель управления и API
   sudo ufw allow 1883/tcp   # MQTT брокер для ESP32
   ```

Все переменные (пароли к InfluxDB, часовой пояс `TZ=Europe/Moscow`) настраиваются в файле `server/.env`.

---

## 📡 MQTT Топики

| Топик | Направление | Формат данных | Описание |
|---|---|---|---|
| `growbox/sensors` | ESP32 → Брокер | JSON | Показания датчиков каждые 30 сек |
| `growbox/status` | ESP32 → Брокер | JSON | Состояние 6 реле, uptime, RSSI, free heap |
| `growbox/image/raw` | ESP32 → Брокер | Base64 / JSON | Кадр 160×120 YUY2 каждые 10 мин или по запросу |
| `growbox/relay/set` | Брокер → ESP32 | `{"relay": 1..6, "state": true/false}` | Переключение реле |
| `growbox/camera/capture` | Брокер → ESP32 | `1` | Команда сделать принудительный снимок |

---

## 📁 Структура репозитория

```
├── growbox/                     # Основная боевая прошивка ESP32-S3
│   ├── growbox.ino              # Главный цикл программы и инициализация
│   ├── config.h                 # Пины, интервалы опроса, константы
│   ├── wifi_config.example.h    # Пример конфигурации Wi-Fi и MQTT
│   ├── sensors.cpp / .h         # Драйверы BME280, DS18B20, MH-Z19B
│   ├── relay.cpp / .h           # Управление 6 LOW-активными реле
│   ├── camera_capture.cpp / .h  # Драйвер UVC камеры на ESP32_USB_STREAM
│   └── mqtt_client.cpp / .h     # Клиент PubSubClient, подписки и публикации
├── server/                      # Серверная Docker-инфраструктура
│   ├── docker-compose.yml       # Конфигурация 4 сервисов
│   ├── deploy.sh                # Скрипт авторазвертывания на Linux VPS
│   ├── .env.example             # Шаблон переменных окружения
│   ├── index.html               # Минималистичный веб-дашборд
│   ├── nginx/default.conf       # Reverse proxy (веб, /api/*, /mqtt)
│   ├── mosquitto/               # Конфигурация Eclipse Mosquitto (1883, 9001)
│   └── bridge/                  # Node.js сервис (YUY2->JPEG, InfluxDB, REST API)
└── stage1_... / stage6_...      # Пошаговые тестовые стенды для отдельных модулей
```

---

## 📜 Лицензия
MIT License. Свободно для личного и коммерческого использования.